/**
 * Public URL for the family product shell. Rewrites to `/family/*` (see next.config).
 * Keeps a stable “.html” entry name without duplicating the `app/family` tree.
 */
export const FAMILY_APP_BASE = "/family-app.html";
